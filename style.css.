body {
  display: flex;
  flex-direction: column;
  align-items: center;
  font-family: Arial, sans-serif;
  background: #f0f0f0;
}

canvas {
  background: #000;
  margin-top: 20px;
  border: 2px solid #555;
}
